package com.example.kbl.githubappcopy.service.enumError;

/**
 * using on callback for inform the error to show
 */
public enum ErrorService {
    ERROR,
    ERROR_NETWORK,
    ERROR_USER,
    ERROR_LOADER,
    ERROR_EMPTY,
    ERROR_REQUEST
}
